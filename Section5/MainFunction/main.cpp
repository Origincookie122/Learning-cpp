#include <iostream>

using namespace std;
/**
 * Testing out using parameters
 * Argc holds the count of the arguments passed to the program(-1)
 * Argv holds a vector array of strings which is the parameters
 * 
 */
int main(int argc, char *argv[]) {
	std::cout << argc << std::endl;

	return 0;
}